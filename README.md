1: instalar severless

pnpm install -g serverless

2: crear proyecto

serverless

seleccionar

AWS / Node.js / Express API

3:

pnpm add -D typescript

inicializar typescript

npx tsc --init

@aws-sdk/client-dynamodb
@aws-sdk/lib-dynamodb

pnpm add uuid

@aws-sdk/client-sns

aws-lambda
@types/aws-lambda

mysql2
@aws-sdk/client-eventbridge

jest
@types/jest
ts-jest
pnpm create jest

pnpm add -D ts-node
